# 10559 — I Love Big Numbers !

## 📘 題目敘述

輸入多個整數 `n`（`n ≤ 1000`）。對每個 `n`，先計算 `n!`，再輸出 `n!` 的數位和。每行一個 `n`，逐行輸出答案。

## 💡 解題提示（Hint）

* 直接以大整數累乘 `1×2×…×n`。Python 內建可處理大數，其他語言需自行實作大數或使用庫。
* 多測資時，先讀完找出最大 `n`，**自底向上**計到 `max_n`，沿途把每個 `i!` 的數位和存表，查表輸出。
* `sum(map(int, str(fact)))` 可快速得到數位和。
* 複雜度約 `O(max_n · 位數(n!))`，每筆查詢 `O(1)`。

## Sample input:

```
5
60
100
```

## Sample Output:

```
3
288
648
```
